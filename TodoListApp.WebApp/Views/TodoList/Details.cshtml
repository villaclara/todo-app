@model TodoList

<div class="container py-4">

    <!-- TodoList Title -->
    <div class="d-flex justify-content-between align-items-center mb-2">
    <h1>@Model.Title</h1>

    <div class="d-flex gap-2">
        <a type="button" class="btn btn-primary" asp-controller="TodoList" asp-action="CreateEdit" asp-route-id="@Model.Id">
           Edit
        </a>

        <a type="button" class="btn btn-danger" asp-controller="TodoList" asp-action="Delete" asp-route-id="@Model.Id">
           Delete
        </a>
    </div>
</div>

    <!-- TodoList Description -->
    <p class="text-muted mb-4">@Model.Description</p>

    <!-- Tasks -->
    @* @if (Model.Tasks == null || !Model.Tasks.Any())
    {
        <p class="text-muted">No tasks in this list.</p>
    } *@
    else
    {
       @*  <div class="row g-3">
            @foreach (var task in Model.Tasks)
            {
                <div class="col-12">
                    <div class="card shadow-sm rounded border-0">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-start">
                                <div>
                                    <!-- Task title and description -->
                                    <h5 class="card-title mb-1">@task.Name</h5>
                                    <p class="card-text text-muted mb-2">@task.Description</p>

                                    <!-- Assignee and due date -->
                                    <div class="text-muted mb-2">
                                        <small>
                                            <strong>Assignee:</strong> @task.Assignee ?? "Unassigned"
                                            &nbsp;|&nbsp;
                                            <strong>Due:</strong> @(task.DueDate?.ToString("MMM dd, yyyy") ?? "No due date")
                                        </small>
                                    </div>

                                    <!-- Tags -->
                                    @if (task.Tags != null && task.Tags.Any())
                                    {
                                        <div>
                                            @foreach (var tag in task.Tags)
                                            {
                                                <span class="badge bg-primary me-1">@tag</span>
                                            }
                                        </div>
                                    }
                                </div>
                                <div class="ms-3 text-muted">
                                    <!-- Optional icon or status could go here -->
                                    <i class="bi bi-clipboard-check fs-4"></i>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div> *@
    }
</div>

<style>
    /* Optional: card hover effect */
    .card:hover {
        transform: translateY(-2px);
        transition: transform 0.2s;
    }

    .badge {
        font-size: 0.8rem;
    }
</style>
